micronaut:
  application:
    name: github-actions-micronaut-demo
datasources:
  default:
    url: jdbc:oracle:thin:@demodb_LOW?TNS_ADMIN=/wallet-demodb
    driverClassName: oracle.jdbc.driver.OracleDriver
    username: cicd
    password: c0v3(r)Te1i+3
    schema-generate: NONE
    dialect: ORACLE
liquibase:
  datasources:
    default:
      change-log: 'classpath:db/liquibase-changelog.xml'